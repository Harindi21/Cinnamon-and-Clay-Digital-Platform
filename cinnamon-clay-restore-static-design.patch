diff --git a/.env.example b/.env.example
index 6054228..773a462 100644
--- a/.env.example
+++ b/.env.example
@@ -7,6 +7,9 @@ POSTGRES_PORT=55432
 BACKEND_PORT=8082
 PUBLIC_WEB_PORT=3000
 NEXT_PUBLIC_SITE_URL=http://localhost:3000
+# Show the original prototype photography whenever managed media has not been
+# uploaded yet. Set false outside demo/local environments.
+DEMO_MEDIA_FALLBACK_ENABLED=true
 
 MINIO_ROOT_USER=cinnamon_clay
 MINIO_ROOT_PASSWORD=change-me-locally
diff --git a/infra/compose.yaml b/infra/compose.yaml
index c8ff1cc..af68081 100644
--- a/infra/compose.yaml
+++ b/infra/compose.yaml
@@ -144,6 +144,7 @@ services:
     environment:
       BACKEND_INTERNAL_URL: http://backend:8080
       PUBLIC_REVALIDATION_SECRET: ${PUBLIC_REVALIDATION_SECRET:-change-me-locally}
+      DEMO_MEDIA_FALLBACK_ENABLED: ${DEMO_MEDIA_FALLBACK_ENABLED:-false}
     ports:
       - "${PUBLIC_WEB_PORT:-3000}:3000"
     security_opt:
diff --git a/public-web/app/globals.css b/public-web/app/globals.css
index 4ada6d8..bd48e53 100644
--- a/public-web/app/globals.css
+++ b/public-web/app/globals.css
@@ -19,7 +19,7 @@
   --radius-md: 18px;
   --radius-lg: 28px;
 
-  --content-width: 1180px;
+  --content-width: 1100px;
 }
 
 *,
@@ -83,7 +83,7 @@ button:focus-visible {
 }
 
 .section {
-  padding-block: 96px;
+  padding-block: 84px;
 }
 
 .sectionKicker,
@@ -138,17 +138,7 @@ button:focus-visible {
   inset: 0;
   z-index: 1;
   background:
-    linear-gradient(
-      180deg,
-      rgba(24, 13, 7, 0.56),
-      rgba(24, 13, 7, 0.48)
-    ),
-    linear-gradient(
-      90deg,
-      rgba(0, 0, 0, 0.22),
-      transparent 50%,
-      rgba(0, 0, 0, 0.12)
-    );
+    rgba(30, 18, 10, 0.55);
   content: "";
 }
 
@@ -161,19 +151,19 @@ button:focus-visible {
   position: relative;
   z-index: 2;
   max-width: 820px;
-  padding-block: 120px 72px;
+  padding: 0 20px;
   text-align: center;
 }
 
 .hero .kicker {
   display: inline-block;
   margin-bottom: 28px;
-  padding: 9px 20px;
-  border: 1px solid rgba(255, 255, 255, 0.3);
+  padding: 8px 18px;
+  border: 1px solid rgba(255, 255, 255, 0.35);
   border-radius: 999px;
-  background: rgba(0, 0, 0, 0.14);
-  color: #e2a762;
-  backdrop-filter: blur(8px);
+  background: rgba(0, 0, 0, 0.15);
+  color: var(--accent);
+  backdrop-filter: blur(4px);
 }
 
 .hero h1 {
@@ -201,11 +191,11 @@ button:focus-visible {
 
 .heroActions a {
   display: inline-flex;
-  min-width: 150px;
-  min-height: 52px;
+  min-width: 0;
+  min-height: 0;
   align-items: center;
   justify-content: center;
-  padding-inline: 28px;
+  padding: 14px 30px;
   border: 1px solid rgba(255, 255, 255, 0.48);
   border-radius: 999px;
   font-weight: 700;
@@ -216,13 +206,14 @@ button:focus-visible {
 }
 
 .heroActions a:first-child {
-  border-color: var(--accent-strong);
-  background: var(--accent-strong);
+  border-color: var(--accent);
+  background: var(--accent);
+  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.25);
 }
 
 .heroActions a:last-child {
-  background: rgba(255, 255, 255, 0.1);
-  backdrop-filter: blur(8px);
+  background: rgba(255, 255, 255, 0.12);
+  backdrop-filter: blur(4px);
 }
 
 .heroActions a:hover {
@@ -230,7 +221,23 @@ button:focus-visible {
 }
 
 .heroActions a:first-child:hover {
-  background: #9a5b20;
+  background: var(--accent);
+}
+
+.scrollHint {
+  position: absolute;
+  bottom: 26px;
+  left: 50%;
+  z-index: 2;
+  color: rgba(255, 255, 255, 0.72);
+  font-size: 1.4rem;
+  transform: translateX(-50%);
+  animation: scroll-bob 2s infinite;
+}
+
+@keyframes scroll-bob {
+  0%, 100% { transform: translate(-50%, 0); }
+  50% { transform: translate(-50%, 8px); }
 }
 
 /* About */
@@ -239,10 +246,8 @@ button:focus-visible {
 
 .aboutSection {
   display: grid;
-  grid-template-columns:
-    minmax(0, 0.9fr)
-    minmax(0, 1.1fr);
-  gap: clamp(48px, 7vw, 88px);
+  grid-template-columns: 1fr 1.1fr;
+  gap: 56px;
   align-items: center;
 }
 
@@ -253,9 +258,10 @@ button:focus-visible {
 
 .aboutMedia {
   position: relative;
-  min-height: 580px;
+  min-height: 0;
+  aspect-ratio: 4 / 4.6;
   overflow: hidden;
-  border-radius: var(--radius-lg);
+  border-radius: var(--radius-md);
   background: var(--surface-soft);
   box-shadow: var(--shadow-md);
 }
@@ -274,7 +280,6 @@ button:focus-visible {
 
 .copy {
   color: var(--muted);
-  font-size: 1.03rem;
 }
 
 .copy > p {
@@ -288,26 +293,26 @@ button:focus-visible {
     minmax(0, 1fr)
   );
   gap: 16px;
-  margin-top: 38px;
+  margin-top: 28px;
 }
 
 .featureCard {
   min-width: 0;
-  padding: 22px 20px;
-  border: 1px solid rgba(92, 58, 33, 0.06);
-  border-radius: var(--radius-md);
+  padding: 18px 16px;
+  border: 0;
+  border-radius: 14px;
   background: var(--surface);
   box-shadow: var(--shadow-sm);
 }
 
 .featureIcon {
   display: block;
-  margin-bottom: 12px;
+  margin-bottom: 0;
   font-size: 1.6rem;
 }
 
 .featureCard h3 {
-  margin-bottom: 7px;
+  margin: 8px 0 4px;
   color: var(--primary);
   font-size: 1.05rem;
 }
@@ -326,7 +331,7 @@ button:focus-visible {
 
 .sectionHeading {
   max-width: 680px;
-  margin: 0 auto 58px;
+  margin: 0 auto 52px;
   text-align: center;
 }
 
@@ -338,7 +343,7 @@ button:focus-visible {
 .menuGrid {
   display: grid;
   grid-template-columns: repeat(2, minmax(0, 1fr));
-  gap: 58px 72px;
+  gap: 44px 64px;
 }
 
 .menuCategory {
@@ -369,7 +374,7 @@ button:focus-visible {
   display: flex;
   justify-content: space-between;
   gap: 20px;
-  padding-block: 14px;
+  padding-block: 11px;
   border-bottom: 1px dashed rgba(124, 106, 92, 0.28);
 }
 
@@ -403,10 +408,8 @@ button:focus-visible {
 
 .galleryGrid {
   display: grid;
-  grid-auto-flow: dense;
-  grid-auto-rows: 210px;
   grid-template-columns: repeat(3, minmax(0, 1fr));
-  gap: 18px;
+  gap: 16px;
 }
 
 .galleryItem {
@@ -414,19 +417,20 @@ button:focus-visible {
   min-width: 0;
   margin: 0;
   overflow: hidden;
-  border-radius: var(--radius-md);
+  aspect-ratio: 1 / 1;
+  border-radius: 14px;
   background: var(--surface-soft);
   box-shadow: var(--shadow-sm);
 }
 
 .galleryItemWide,
 .galleryItemFeatured {
-  grid-column: span 2;
+  grid-column: auto;
 }
 
 .galleryItemTall,
 .galleryItemFeatured {
-  grid-row: span 2;
+  grid-row: auto;
 }
 
 .galleryOpenButton {
@@ -646,16 +650,16 @@ button:focus-visible {
     3,
     minmax(0, 1fr)
   );
-  gap: 22px;
+  gap: 20px;
 }
 
 .reviewCard {
   display: flex;
   min-width: 0;
   flex-direction: column;
-  gap: 18px;
-  padding: 30px;
-  border: 1px solid var(--border);
+  gap: 12px;
+  padding: 28px;
+  border: 0;
   border-radius: var(--radius-md);
   background: var(--background);
   box-shadow: var(--shadow-sm);
@@ -671,9 +675,9 @@ button:focus-visible {
   flex: 1;
   margin: 0;
   color: var(--ink);
-  font-family: var(--font-serif), Georgia, serif;
-  font-size: 1.08rem;
-  line-height: 1.7;
+  font-family: var(--font-sans), Arial, Helvetica, sans-serif;
+  font-size: 0.95rem;
+  line-height: 1.65;
 }
 
 .reviewAuthor {
@@ -685,43 +689,48 @@ button:focus-visible {
 
 /* Visit */
 
-.visit {
+.visitSection {
+  background: var(--background);
+}
+
+.visitGrid {
   display: grid;
-  grid-template-columns: minmax(0, 1.15fr) minmax(300px, 0.85fr);
-  gap: clamp(42px, 7vw, 90px);
-  align-items: center;
+  grid-template-columns: 1.1fr 1fr;
+  gap: 44px;
+  align-items: stretch;
 }
 
 .mapFrame {
   width: 100%;
-  min-height: 410px;
-  margin-top: 26px;
+  min-height: 340px;
+  margin-top: 0;
   border: 0;
   border-radius: var(--radius-md);
   background: var(--surface-soft);
-  box-shadow: var(--shadow-md);
+  box-shadow: var(--shadow-sm);
 }
 
 .visitDetails {
-  padding: 34px;
-  border: 1px solid var(--border);
-  border-radius: var(--radius-lg);
-  background: rgba(255, 255, 255, 0.58);
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+  gap: 22px;
+  padding: 0;
+  border: 0;
+  background: transparent;
 }
 
-.visitDetails h3 {
-  margin-top: 28px;
-  margin-bottom: 7px;
+.visitBlock h3 {
+  margin: 0 0 8px;
   color: var(--primary);
-  font-size: 1.15rem;
+  font-size: 1.1rem;
 }
 
-.visitDetails h3:first-child {
-  margin-top: 0;
-}
-
-.visitDetails p {
+.visitBlock p,
+.visitBlock a {
+  margin: 0;
   color: var(--muted);
+  font-size: 0.95rem;
 }
 
 .visitDetails a {
@@ -754,57 +763,85 @@ button:focus-visible {
   text-align: right;
 }
 
-.socialLinks {
-  display: flex;
-  flex-wrap: wrap;
-  gap: 10px;
-  margin-top: 24px;
+/* Footer */
+
+footer {
+  padding-block: 44px;
+  background: var(--primary);
+  color: rgba(255, 255, 255, 0.78);
+  text-align: center;
+  font-size: 0.88rem;
 }
 
-.socialLinks a {
-  padding: 8px 14px;
-  border: 1px solid var(--border);
-  border-radius: 999px;
-  color: var(--primary);
-  font-size: 0.86rem;
-  font-weight: 700;
-  text-transform: capitalize;
+.footerInner p {
+  margin-bottom: 0;
 }
 
-.socialLinks a:hover {
-  border-color: var(--accent-strong);
+.footerBrand {
+  color: #ffffff;
+  font-family: var(--font-serif), Georgia, serif;
+  font-size: 1.4rem;
+  font-weight: 600;
 }
 
-.whatsappLink {
-  display: inline-flex;
-  min-height: 46px;
+.footerSocials {
+  display: flex;
+  justify-content: center;
+  gap: 18px;
+  margin: 18px 0;
+}
+
+.footerSocials a {
+  display: flex;
+  width: 42px;
+  height: 42px;
   align-items: center;
-  margin-top: 24px;
-  padding-inline: 20px;
-  border-radius: 999px;
-  background: var(--primary);
-  color: #ffffff;
-  font-size: 0.9rem;
-  font-weight: 700;
-  transition:
-    background-color 150ms ease,
-    transform 150ms ease;
+  justify-content: center;
+  border-radius: 50%;
+  background: rgba(255, 255, 255, 0.12);
+  font-size: 1.1rem;
+  transition: background 0.2s, transform 0.2s;
 }
 
-.whatsappLink:hover {
-  background: #714829;
-  color: #ffffff;
-  transform: translateY(-1px);
+.footerSocials a:hover {
+  background: var(--accent);
+  transform: translateY(-2px);
 }
 
-/* Footer */
+.footerCredit {
+  margin-top: 10px !important;
+  color: rgba(255, 255, 255, 0.55);
+  font-size: 0.8rem;
+}
 
-footer {
-  padding-block: 42px;
-  background: var(--primary);
-  color: rgba(255, 255, 255, 0.78);
-  text-align: center;
-  font-size: 0.88rem;
+.footerCredit a {
+  text-decoration: underline;
+}
+
+.whatsappFloat {
+  position: fixed;
+  right: 24px;
+  bottom: 24px;
+  z-index: 40;
+  display: flex;
+  width: 60px;
+  height: 60px;
+  align-items: center;
+  justify-content: center;
+  border-radius: 50%;
+  background: #25d366;
+  box-shadow: 0 8px 24px rgba(37, 211, 102, 0.45);
+  transition: transform 0.2s;
+}
+
+.whatsappFloat:hover {
+  transform: scale(1.08);
+}
+
+.whatsappFloat svg {
+  width: 32px;
+  height: 32px;
+  fill: #ffffff;
 }
 
 /* Tablet */
@@ -829,6 +866,10 @@ footer {
   aspect-ratio: 4 / 3;
 }
 
+.visitGrid {
+  grid-template-columns: 1fr;
+}
+
 .featureGrid {
   grid-template-columns: repeat(
     3,
@@ -871,7 +912,6 @@ footer {
   }
 
   .galleryGrid {
-    grid-auto-rows: 180px;
     grid-template-columns: 1fr;
   }
 
@@ -883,7 +923,8 @@ footer {
   }
 
   .galleryItem {
-    min-height: 260px;
+    min-height: 0;
+    aspect-ratio: 1 / 1;
   }
 
   .galleryZoomHint,
@@ -919,10 +960,6 @@ footer {
     min-height: 320px;
   }
 
-  .visitDetails {
-    padding: 24px 20px;
-  }
-
   .hoursRow {
     flex-direction: column;
     gap: 2px;
@@ -1006,13 +1043,22 @@ footer {
   inset: 0 0 auto;
   z-index: 50;
   color: #ffffff;
-  background: rgba(44, 30, 20, 0.18);
-  border-bottom: 1px solid rgba(255, 255, 255, 0.14);
-  backdrop-filter: blur(10px);
+  background: transparent;
+  border-bottom: 1px solid transparent;
+  transition:
+    background 0.3s,
+    box-shadow 0.3s,
+    border-color 0.3s;
+}
+
+.siteNavScrolled {
+  background: var(--background);
+  border-bottom-color: rgba(92, 58, 33, 0.08);
+  box-shadow: 0 2px 20px rgba(44, 30, 20, 0.1);
 }
 
 .siteNavInner {
-  min-height: 72px;
+  min-height: 76px;
   display: flex;
   align-items: center;
   justify-content: space-between;
@@ -1028,6 +1074,7 @@ footer {
   font-weight: 600;
   color: #ffffff;
   white-space: nowrap;
+  transition: color 0.3s;
 }
 
 .siteNavMenu {
@@ -1040,11 +1087,16 @@ footer {
   color: rgba(255, 255, 255, 0.94);
   font-size: 0.9rem;
   font-weight: 600;
-  transition: color 150ms ease;
+  transition: color 0.3s;
+}
+
+.siteNavScrolled .siteBrand,
+.siteNavScrolled .siteNavMenu a {
+  color: var(--ink);
 }
 
 .siteNavMenu a:hover {
-  color: var(--accent-strong);
+  color: var(--accent);
 }
 
 .siteNavToggle {
@@ -1067,6 +1119,15 @@ footer {
   transition: transform 150ms ease, opacity 150ms ease;
 }
 
+.siteNavScrolled .siteNavToggle {
+  border-color: rgba(44, 30, 20, 0.2);
+  background: transparent;
+}
+
+.siteNavScrolled .siteNavToggle span {
+  background: var(--primary);
+}
+
 .siteNavOpen .siteNavToggle span:nth-child(1) {
   transform: translateY(7px) rotate(45deg);
 }
@@ -1095,7 +1156,7 @@ footer {
     justify-content: center;
     gap: 28px;
     padding: 90px 24px 40px;
-    background: var(--bg);
+    background: var(--background);
     transform: translateX(100%);
     transition: transform 180ms ease;
   }
diff --git a/public-web/app/page.tsx b/public-web/app/page.tsx
index 1ffae57..21bcf5f 100644
--- a/public-web/app/page.tsx
+++ b/public-web/app/page.tsx
@@ -1,354 +1,290 @@
 import Image from 'next/image';
-import { Gallery } from '@/components/gallery';
-import { SiteNav } from '@/components/site-nav';
 import { connection } from 'next/server';
-import { getPublicReviews } from '@/lib/reviews';
 
+import { Gallery } from '@/components/gallery';
+import { SiteNav } from '@/components/site-nav';
 import { formatMoney, getMenu } from '@/lib/catalog';
-import {
-  getContact,
-  getSiteContent,
-  whatsappHref
-} from '@/lib/site';
+import { withDemoMediaFallback } from '@/lib/demo-media';
 import {
   getPublicMedia,
   mediaSrc,
   objectPosition
 } from '@/lib/media';
+import { getPublicReviews } from '@/lib/reviews';
+import {
+  getContact,
+  getSiteContent,
+  whatsappHref
+} from '@/lib/site';
 
 export default async function Home() {
   await connection();
 
-const [
-  menu,
-  content,
-  contact,
-  media,
-  reviewData
-] = await Promise.all([
-  getMenu(),
-  getSiteContent(),
-  getContact(),
-  getPublicMedia(),
-  getPublicReviews()
-]);
+  const [menu, content, contact, media, reviewData] = await Promise.all([
+    getMenu(),
+    getSiteContent(),
+    getContact(),
+    getPublicMedia(),
+    getPublicReviews()
+  ]);
 
+  const displayMedia = withDemoMediaFallback(media);
   const whatsAppUrl = whatsappHref(contact.whatsapp);
 
   return (
     <main id="top">
       <SiteNav brand={content.brand.name} />
 
-      {/* Hero */}
-      <section className="hero">
-        {media.hero && (
+      <section className="hero" aria-labelledby="hero-title">
+        {displayMedia.hero && (
           <Image
             className="heroMedia"
-            src={mediaSrc(media.hero)}
-            alt={media.hero.alt}
+            src={mediaSrc(displayMedia.hero)}
+            alt={displayMedia.hero.alt}
             fill
             priority
             sizes="100vw"
-            style={{ objectPosition: objectPosition(media.hero) }}
+            style={{ objectPosition: objectPosition(displayMedia.hero) }}
           />
         )}
 
         <div className="shell heroContent">
-          <p className="kicker">
-            {content.brand.heroNote}
-          </p>
-
-          <h1>{content.brand.name}</h1>
-
-          <p className="tagline">
-            {content.brand.tagline}
-          </p>
+          <p className="kicker">{content.brand.heroNote}</p>
+          <h1 id="hero-title">{content.brand.name}</h1>
+          <p className="tagline">{content.brand.tagline}</p>
 
           <div className="heroActions">
-            <a href="#menu">
-              View Menu
-            </a>
-
-            <a href="#visit">
-              Find Us
-            </a>
+            <a href="#menu">View Menu</a>
+            <a href="#visit">Find Us</a>
           </div>
         </div>
+
+        <a className="scrollHint" href="#about" aria-label="Scroll to About">
+          ↓
+        </a>
       </section>
 
-      {/* About */}
       <section
         id="about"
         className={
-          media.about
+          displayMedia.about
             ? 'section shell aboutSection'
             : 'section shell aboutSection aboutSectionWithoutMedia'
         }
       >
-        {media.about && (
+        {displayMedia.about && (
           <div className="aboutMedia">
             <Image
-              src={mediaSrc(media.about)}
-              alt={media.about.alt}
+              src={mediaSrc(displayMedia.about)}
+              alt={displayMedia.about.alt}
               fill
               sizes="(max-width: 900px) 100vw, 45vw"
-              style={{ objectPosition: objectPosition(media.about) }}
+              style={{ objectPosition: objectPosition(displayMedia.about) }}
             />
           </div>
         )}
 
         <div className="aboutContent">
-          <p className="sectionKicker">
-            Who we are
-          </p>
-
+          <p className="sectionKicker">Who we are</p>
           <h2>{content.about.title}</h2>
 
           <div className="copy">
-            {content.about.paragraphs.map(
-              (paragraph) => (
-                <p key={paragraph}>
-                  {paragraph}
-                </p>
-              )
-            )}
+            {content.about.paragraphs.map((paragraph) => (
+              <p key={paragraph}>{paragraph}</p>
+            ))}
 
             <div className="featureGrid">
-              {content.about.features.map(
-                (feature) => (
-                  <article
-                    className="featureCard"
-                    key={feature.title}
-                  >
-                    <span className="featureIcon" aria-hidden="true">
-                      {feature.icon}
-                    </span>
-
-                    <h3>{feature.title}</h3>
-
-                    <p>{feature.text}</p>
-                  </article>
-                )
-              )}
+              {content.about.features.map((feature) => (
+                <article className="featureCard" key={feature.title}>
+                  <span className="featureIcon" aria-hidden="true">
+                    {feature.icon}
+                  </span>
+                  <h3>{feature.title}</h3>
+                  <p>{feature.text}</p>
+                </article>
+              ))}
             </div>
           </div>
         </div>
       </section>
 
-      {/* Menu */}
       <section id="menu" className="section">
         <div className="shell">
           <div className="sectionHeading">
-            <p className="sectionKicker">
-              Good things to order
-            </p>
-
+            <p className="sectionKicker">Good things to order</p>
             <h2>The Menu</h2>
-
             <p>{content.menuNote}</p>
           </div>
 
           <div className="menuGrid">
-            {menu.categories.map(
-              (category) => (
-                <article
-                  key={category.id}
-                  className="menuCategory"
-                >
-                  <h3>{category.name}</h3>
-
-                  <div>
-                    {category.items.map(
-                      (item) => (
-                        <div
-                          className="menuItem"
-                          key={item.id}
-                        >
-                          <div>
-                            <h4>{item.name}</h4>
-                            <p>{item.description}</p>
-                          </div>
-
-                          <strong>
-                            {formatMoney(item.price)}
-                          </strong>
-                        </div>
-                      )
-                    )}
-                  </div>
-                </article>
-              )
-            )}
+            {menu.categories.map((category) => (
+              <article key={category.id} className="menuCategory">
+                <h3>{category.name}</h3>
+                <div>
+                  {category.items.map((item) => (
+                    <div className="menuItem" key={item.id}>
+                      <div>
+                        <h4>{item.name}</h4>
+                        <p>{item.description}</p>
+                      </div>
+                      <strong>{formatMoney(item.price)}</strong>
+                    </div>
+                  ))}
+                </div>
+              </article>
+            ))}
           </div>
         </div>
       </section>
 
-      {/* Gallery */}
-      {media.gallery.length > 0 && (
-        <section
-          id="gallery"
-          className="section gallerySection"
-        >
+      {displayMedia.gallery.length > 0 && (
+        <section id="gallery" className="section gallerySection">
           <div className="shell">
             <div className="sectionHeading">
-              <p className="sectionKicker">
-                A peek inside
-              </p>
-
+              <p className="sectionKicker">A peek inside</p>
               <h2>Gallery</h2>
             </div>
-
-            <Gallery assets={media.gallery} />
+            <Gallery assets={displayMedia.gallery} />
           </div>
         </section>
       )}
 
-{/* Reviews */}
-{reviewData.reviews.length > 0 && (
-  <section
-    id="reviews"
-    className="section reviewsSection"
-  >
-    <div className="shell">
-      <div className="sectionHeading">
-        <p className="sectionKicker">
-          Kind words
-        </p>
-
-        <h2>What People Say</h2>
-      </div>
-
-      <div className="reviewsGrid">
-        {reviewData.reviews.map(
-          (review) => (
-            <article
-              className="reviewCard"
-              key={review.id}
-            >
-              <div
-                className="reviewStars"
-                role="img"
-                aria-label={
-                  `${review.rating} out of 5 stars`
-                }
-              >
-                <span aria-hidden="true">
-                  {'★'.repeat(review.rating)}
-                  {'☆'.repeat(
-                    5 - review.rating
-                  )}
-                </span>
-              </div>
-
-              <blockquote>
-                “{review.body}”
-              </blockquote>
-
-              <p className="reviewAuthor">
-                — {review.authorName}
-              </p>
-            </article>
-          )
-        )}
-      </div>
-    </div>
-  </section>
-)}
+      {reviewData.reviews.length > 0 && (
+        <section id="reviews" className="section reviewsSection">
+          <div className="shell">
+            <div className="sectionHeading">
+              <p className="sectionKicker">Kind words</p>
+              <h2>What People Say</h2>
+            </div>
 
-      {/* Visit */}
-      <section
-        id="visit"
-        className="section shell visit"
-      >
-        <div>
-          <p className="sectionKicker">
-            Come say hi
-          </p>
+            <div className="reviewsGrid">
+              {reviewData.reviews.map((review) => (
+                <article className="reviewCard" key={review.id}>
+                  <div
+                    className="reviewStars"
+                    role="img"
+                    aria-label={`${review.rating} out of 5 stars`}
+                  >
+                    <span aria-hidden="true">
+                      {'★'.repeat(review.rating)}
+                      {'☆'.repeat(5 - review.rating)}
+                    </span>
+                  </div>
+                  <blockquote>“{review.body}”</blockquote>
+                  <p className="reviewAuthor">— {review.authorName}</p>
+                </article>
+              ))}
+            </div>
+          </div>
+        </section>
+      )}
 
-          <h2>Visit Us</h2>
+      <section id="visit" className="section visitSection">
+        <div className="shell">
+          <div className="sectionHeading">
+            <p className="sectionKicker">Come say hi</p>
+            <h2>Visit Us</h2>
+          </div>
 
-          {contact.mapEmbedUrl && (
-            <iframe
-              className="mapFrame"
-              src={contact.mapEmbedUrl}
-              loading="lazy"
-              title={`${content.brand.name} location`}
-            />
-          )}
-        </div>
+          <div className="visitGrid">
+            {contact.mapEmbedUrl && (
+              <iframe
+                className="mapFrame"
+                src={contact.mapEmbedUrl}
+                loading="lazy"
+                title={`${content.brand.name} location`}
+              />
+            )}
 
-        <div className="visitDetails">
-          <h3>Address</h3>
-          <p>{contact.address}</p>
+            <div className="visitDetails">
+              <div className="visitBlock">
+                <h3>📍 Address</h3>
+                <p>{contact.address}</p>
+              </div>
 
-          <h3>Opening Hours</h3>
+              <div className="visitBlock">
+                <h3>🕒 Opening Hours</h3>
+                <div className="hoursList">
+                  {contact.hours.map((hour) => (
+                    <div className="hoursRow" key={hour.day}>
+                      <span>{hour.day}</span>
+                      <span>{hour.time}</span>
+                    </div>
+                  ))}
+                </div>
+              </div>
 
-          <div className="hoursList">
-            {contact.hours.map((hour) => (
-              <div
-                className="hoursRow"
-                key={hour.day}
-              >
-                <span>{hour.day}</span>
-                <span>{hour.time}</span>
+              <div className="visitBlock">
+                <h3>📞 Contact</h3>
+                <p>
+                  <a href={`tel:${contact.phone.replace(/[^\d+]/g, '')}`}>
+                    {contact.phone}
+                  </a>
+                  <br />
+                  <a href={`mailto:${contact.email}`}>{contact.email}</a>
+                </p>
               </div>
-            ))}
+            </div>
           </div>
+        </div>
+      </section>
 
-          <h3>Contact</h3>
-
-          <p>
-            <a
-              href={`tel:${contact.phone.replace(
-                /[^\d+]/g,
-                ''
-              )}`}
-            >
-              {contact.phone}
-            </a>
-
-            <br />
-
-            <a href={`mailto:${contact.email}`}>
-              {contact.email}
-            </a>
-          </p>
+      <footer>
+        <div className="shell footerInner">
+          <div className="footerBrand">{content.brand.name}</div>
 
-          <div className="socialLinks">
-            {contact.socialLinks.map(
-              (link) => (
+          {contact.socialLinks.length > 0 && (
+            <div className="footerSocials" aria-label="Social links">
+              {contact.socialLinks.map((link) => (
                 <a
                   key={link.platform}
                   href={link.url}
                   target="_blank"
                   rel="noopener noreferrer"
+                  aria-label={link.platform}
+                  title={link.platform}
                 >
-                  {link.platform}
+                  {socialIcon(link.platform)}
                 </a>
-              )
-            )}
-          </div>
-
-          {whatsAppUrl && (
-            <a
-              className="whatsappLink"
-              href={whatsAppUrl}
-              target="_blank"
-              rel="noopener noreferrer"
-            >
-              Order on WhatsApp
-            </a>
+              ))}
+            </div>
           )}
-        </div>
-      </section>
 
-      {/* Footer */}
-      <footer>
-        <div className="shell">
-          © {new Date().getFullYear()}{' '}
-          {content.brand.name}
+          <p>
+            © {new Date().getFullYear()} {content.brand.name}. All rights reserved.
+          </p>
+          <p className="footerCredit">
+            <a href="mailto:eranthikah999@gmail.com">Site crafted by Hari</a>
+          </p>
         </div>
       </footer>
+
+      {whatsAppUrl && (
+        <a
+          className="whatsappFloat"
+          href={whatsAppUrl}
+          target="_blank"
+          rel="noopener noreferrer"
+          aria-label="Order on WhatsApp"
+        >
+          <svg viewBox="0 0 32 32" aria-hidden="true">
+            <path d="M16.04 3C9.02 3 3.32 8.7 3.32 15.72c0 2.24.59 4.43 1.71 6.36L3.2 29l7.1-1.86a12.66 12.66 0 0 0 5.73 1.38h.01c7.01 0 12.72-5.7 12.72-12.72S23.05 3 16.04 3zm0 23.4h-.01a10.6 10.6 0 0 1-5.4-1.48l-.39-.23-4.21 1.1 1.12-4.1-.25-.42a10.62 10.62 0 0 1-1.63-5.65c0-5.86 4.77-10.63 10.64-10.63a10.6 10.6 0 0 1 10.63 10.64c0 5.86-4.77 10.63-10.63 10.63zm5.83-7.96c-.32-.16-1.89-.93-2.18-1.04-.29-.11-.5-.16-.72.16-.21.32-.82 1.04-1.01 1.25-.19.21-.37.24-.69.08-.32-.16-1.35-.5-2.57-1.59-.95-.85-1.59-1.9-1.78-2.22-.19-.32-.02-.49.14-.65.14-.14.32-.37.48-.56.16-.19.21-.32.32-.53.11-.21.05-.4-.03-.56-.08-.16-.72-1.73-.98-2.37-.26-.62-.52-.54-.72-.55h-.61c-.21 0-.56.08-.85.4-.29.32-1.12 1.09-1.12 2.66s1.15 3.08 1.31 3.29c.16.21 2.25 3.44 5.45 4.82.76.33 1.36.53 1.82.67.77.24 1.46.21 2.01.13.61-.09 1.89-.77 2.16-1.52.27-.75.27-1.39.19-1.52-.08-.13-.29-.21-.61-.37z" />
+          </svg>
+        </a>
+      )}
     </main>
   );
-}
\ No newline at end of file
+}
+
+function socialIcon(platform: string): string {
+  switch (platform.toLowerCase()) {
+    case 'instagram':
+      return '📸';
+    case 'facebook':
+      return '👍';
+    case 'tiktok':
+      return '🎵';
+    default:
+      return '↗';
+  }
+}
diff --git a/public-web/components/site-nav.tsx b/public-web/components/site-nav.tsx
index bcc61d4..d1e817d 100644
--- a/public-web/components/site-nav.tsx
+++ b/public-web/components/site-nav.tsx
@@ -12,9 +12,17 @@ const links = [
 
 export function SiteNav({ brand }: { brand: string }) {
   const [open, setOpen] = useState(false);
+  const [scrolled, setScrolled] = useState(false);
   const menuRef = useRef<HTMLDivElement>(null);
   const toggleRef = useRef<HTMLButtonElement>(null);
 
+  useEffect(() => {
+    const onScroll = () => setScrolled(window.scrollY > 40);
+    onScroll();
+    window.addEventListener('scroll', onScroll, { passive: true });
+    return () => window.removeEventListener('scroll', onScroll);
+  }, []);
+
   useEffect(() => {
     if (!open) {
       return;
@@ -41,7 +49,10 @@ export function SiteNav({ brand }: { brand: string }) {
   }, [open]);
 
   return (
-    <nav className={`siteNav${open ? ' siteNavOpen' : ''}`} aria-label="Primary">
+    <nav
+      className={`siteNav${scrolled ? ' siteNavScrolled' : ''}${open ? ' siteNavOpen' : ''}`}
+      aria-label="Primary"
+    >
       <div className="shell siteNavInner">
         <a
           className="siteBrand"
diff --git a/public-web/lib/catalog.ts b/public-web/lib/catalog.ts
index 7468cdc..361d75e 100644
--- a/public-web/lib/catalog.ts
+++ b/public-web/lib/catalog.ts
@@ -29,6 +29,12 @@ export function getMenu(): Promise<MenuResponse> {
 }
 
 export function formatMoney(money: Money): string {
+  if (money.currency.toUpperCase() === 'LKR') {
+    return `Rs. ${new Intl.NumberFormat('en-LK', {
+      maximumFractionDigits: 0
+    }).format(money.amountMinor / 100)}`;
+  }
+
   return new Intl.NumberFormat('en-LK', {
     style: 'currency',
     currency: money.currency,
diff --git a/public-web/lib/demo-media.ts b/public-web/lib/demo-media.ts
new file mode 100644
index 0000000..69a9d6d
--- /dev/null
+++ b/public-web/lib/demo-media.ts
@@ -0,0 +1,115 @@
+import type { MediaAsset, PublicMedia } from '@/lib/media';
+
+const demoAsset = (
+  id: string,
+  url: string,
+  alt: string,
+  caption: string,
+  focalXPercent = 50,
+  focalYPercent = 50
+): MediaAsset => ({
+  id,
+  url,
+  alt,
+  caption,
+  focalXPercent,
+  focalYPercent,
+  width: null,
+  height: null,
+  version: 1
+});
+
+const DEMO_HERO = demoAsset(
+  'demo-hero',
+  'https://images.unsplash.com/photo-1554118811-1e0d58224f24?w=1800&q=85&auto=format&fit=crop',
+  'Warm cafe interior with tables and natural light',
+  'A warm corner for slow coffee and good company.',
+  50,
+  48
+);
+
+const DEMO_ABOUT = demoAsset(
+  'demo-about',
+  'https://images.unsplash.com/photo-1521017432531-fbd92d768814?w=1200&q=85&auto=format&fit=crop',
+  'Cafe counter and seating area',
+  'Built around coffee, bakes and unhurried conversations.'
+);
+
+const DEMO_GALLERY = [
+  demoAsset(
+    'demo-gallery-coffee-cup',
+    'https://images.unsplash.com/photo-1509042239860-f550ce710b93?w=1200&q=85&auto=format&fit=crop',
+    'Cup of freshly brewed coffee on a cafe table',
+    'Coffee made for slow mornings.',
+    50,
+    52
+  ),
+  demoAsset(
+    'demo-gallery-pour-over',
+    'https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?w=1200&q=85&auto=format&fit=crop',
+    'Coffee being prepared by hand',
+    'Care in every pour.',
+    52,
+    48
+  ),
+  demoAsset(
+    'demo-gallery-table',
+    'https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=1200&q=85&auto=format&fit=crop',
+    'Cafe table prepared for food and drinks',
+    'A table worth lingering around.'
+  ),
+  demoAsset(
+    'demo-gallery-beans',
+    'https://images.unsplash.com/photo-1447933601403-0c6688de566e?w=1200&q=85&auto=format&fit=crop',
+    'Roasted coffee beans ready for brewing',
+    'Fresh beans, carefully brewed.'
+  ),
+  demoAsset(
+    'demo-gallery-latte',
+    'https://images.unsplash.com/photo-1461023058943-07fcbe16d735?w=1200&q=85&auto=format&fit=crop',
+    'Milk coffee served in a ceramic cup',
+    'Comfort in a cup.',
+    50,
+    55
+  ),
+  demoAsset(
+    'demo-gallery-brunch',
+    'https://images.unsplash.com/photo-1504754524776-8f4f37790ca0?w=1200&q=85&auto=format&fit=crop',
+    'Brunch dishes arranged on a shared table',
+    'Something warm to go with your coffee.',
+    50,
+    52
+  )
+] as const;
+
+/**
+ * Keep the real platform media model intact while making a fresh local clone
+ * look like the original static Cinnamon & Clay prototype before an editor has
+ * uploaded media. Production continues to respect the CMS/media state unless
+ * DEMO_MEDIA_FALLBACK_ENABLED=true is explicitly supplied.
+ */
+export function withDemoMediaFallback(media: PublicMedia): PublicMedia {
+  if (!demoFallbackEnabled()) {
+    return media;
+  }
+
+  return {
+    hero: media.hero ?? DEMO_HERO,
+    about: media.about ?? DEMO_ABOUT,
+    gallery: media.gallery.length > 0 ? media.gallery : [...DEMO_GALLERY]
+  };
+}
+
+function demoFallbackEnabled(): boolean {
+  const configured = process.env.DEMO_MEDIA_FALLBACK_ENABLED?.trim().toLowerCase();
+
+  if (configured === 'true') {
+    return true;
+  }
+
+  if (configured === 'false') {
+    return false;
+  }
+
+  return process.env.NODE_ENV !== 'production';
+}
diff --git a/public-web/lib/media.ts b/public-web/lib/media.ts
index f8dc398..3a02fec 100644
--- a/public-web/lib/media.ts
+++ b/public-web/lib/media.ts
@@ -23,6 +23,10 @@ export function getPublicMedia(): Promise<PublicMedia> {
 }
 
 export function mediaSrc(asset: MediaAsset): string {
+  if (/^https?:\/\//i.test(asset.url)) {
+    return asset.url;
+  }
+
   return `/media/${encodeURIComponent(asset.id)}?v=${asset.version}`;
 }
 
diff --git a/public-web/next.config.ts b/public-web/next.config.ts
index c2133bb..5cfd98c 100644
--- a/public-web/next.config.ts
+++ b/public-web/next.config.ts
@@ -3,6 +3,14 @@ import type { NextConfig } from 'next';
 const nextConfig: NextConfig = {
   output: 'standalone',
   poweredByHeader: false,
+  images: {
+    remotePatterns: [
+      {
+        protocol: 'https',
+        hostname: 'images.unsplash.com'
+      }
+    ]
+  },
   turbopack: {
     // The monorepo root has its own lockfile for commit tooling. Explicitly
     // scope Turbopack to this application so Next.js does not infer the wrong
